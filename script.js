//showStage(1);

// // Function to switch between stages
// function showStage(stageNumber) {
//     const stages = document.querySelectorAll('.form-stage');

//     stages.forEach((stage, index) => {
//         if (index + 1 === stageNumber) {
//             stage.classList.add('active');
//             stage.style.display = 'block';
//         } else {
//             stage.classList.remove('active');
//             stage.style.display = 'none';
//         }
//     });

//     addFieldListeners(); // Add listeners for the new active stage
//     updateNextButtonState(); // Update the button state for the new active stage
// }
